# Edge Cases & Failure Modes

- What happens on retry (duplicate requests)
- What happens with malformed/partial input
- What happens when a downstream dependency is unavailable
- What happens under concurrent writes to the same entity
