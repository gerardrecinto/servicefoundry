# Problem

One paragraph, as it would arrive from a product manager. Deliberately underspecified — no entities, no scale numbers, no constraints. That ambiguity is the point; resolving it is step one.
